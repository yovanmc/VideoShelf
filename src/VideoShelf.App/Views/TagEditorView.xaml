<UserControl x:Class="VideoShelf.App.Views.TagEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/VideoShelf.App;component/Resources/DesignTokens.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <StackPanel>

        <!-- Applied tags (own tags with remove button) -->
        <ItemsControl ItemsSource="{Binding Tags}">
            <ItemsControl.ItemsPanel><ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource Chip}" Margin="0,0,6,4">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding}" VerticalAlignment="Center" Margin="0,0,4,0"/>
                            <ui:Button Style="{StaticResource TertiaryButton}"
                                       Command="{Binding DataContext.RemoveTagCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                       CommandParameter="{Binding}"
                                       ToolTip="Remove tag"
                                       AutomationProperties.Name="Remove tag">
                                <ui:SymbolIcon Symbol="Dismiss24" />
                            </ui:Button>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Inherited tags (read-only, from parent level) -->
        <ItemsControl ItemsSource="{Binding Inherited}" Margin="0,2,0,0">
            <ItemsControl.ItemsPanel><ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource Chip}" Margin="0,0,6,4" Opacity="0.6">
                        <TextBlock VerticalAlignment="Center">
                            <Run Text="{Binding Tag, Mode=OneWay}"/>
                            <Run Text=" · " Foreground="{StaticResource TextFillColorTertiaryBrush}"/>
                            <Run Text="{Binding SourceLabel, Mode=OneWay}" Foreground="{StaticResource TextFillColorTertiaryBrush}"/>
                        </TextBlock>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Input row -->
        <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
            <ui:TextBox Text="{Binding TagInput, UpdateSourceTrigger=PropertyChanged}"
                        PlaceholderText="Add tag…" Width="180"/>
            <ui:Button Content="Add" Command="{Binding AddTagCommand}" Margin="8,0,0,0"
                       Style="{StaticResource SecondaryButton}"/>
        </StackPanel>

        <!-- Suggestions -->
        <ItemsControl ItemsSource="{Binding Suggestions}" Margin="0,4,0,0">
            <ItemsControl.ItemsPanel><ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding}" Margin="0,0,6,4"
                            Command="{Binding DataContext.AddSuggestionCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                            CommandParameter="{Binding}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

    </StackPanel>
</UserControl>
