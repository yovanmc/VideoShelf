<UserControl x:Class="VideoShelf.App.Views.CreatorCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vis="clr-namespace:VideoShelf.App.Visuals">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/VideoShelf.App;component/Resources/DesignTokens.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <vis:StringToInitialsConverter x:Key="StringToInitialsConv" />
            <vis:StringToAvatarBrushConverter x:Key="StringToAvatarBrushConv" />
            <vis:NullToVisibilityConverter x:Key="NullToVisibility" />
        </ResourceDictionary>
    </UserControl.Resources>
    <Button Command="{Binding OpenCommand}" Width="{StaticResource CardWidth}" Padding="0"
            Background="Transparent" BorderThickness="0" Cursor="Hand"
            HorizontalContentAlignment="Stretch"
            FocusVisualStyle="{StaticResource AppFocusVisual}"
            RenderTransformOrigin="0.5,0.5">
        <Button.Effect>
            <DropShadowEffect BlurRadius="16" ShadowDepth="2" Direction="270"
                              Color="#000000" Opacity="0"/>
        </Button.Effect>
        <Button.RenderTransform>
            <ScaleTransform ScaleX="1" ScaleY="1"/>
        </Button.RenderTransform>
        <Button.Style>
            <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Trigger.EnterActions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                                                     To="1.03" Duration="{StaticResource AnimFast}">
                                        <DoubleAnimation.EasingFunction><StaticResource ResourceKey="EaseOut"/></DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                                                     To="1.03" Duration="{StaticResource AnimFast}">
                                        <DoubleAnimation.EasingFunction><StaticResource ResourceKey="EaseOut"/></DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                    <DoubleAnimation Storyboard.TargetProperty="Effect.Opacity"
                                                     To="0.45" Duration="{StaticResource AnimFast}">
                                        <DoubleAnimation.EasingFunction><StaticResource ResourceKey="EaseOut"/></DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </Trigger.EnterActions>
                        <Trigger.ExitActions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                                                     To="1" Duration="{StaticResource AnimFast}">
                                        <DoubleAnimation.EasingFunction><StaticResource ResourceKey="EaseOut"/></DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                                                     To="1" Duration="{StaticResource AnimFast}">
                                        <DoubleAnimation.EasingFunction><StaticResource ResourceKey="EaseOut"/></DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                    <DoubleAnimation Storyboard.TargetProperty="Effect.Opacity"
                                                     To="0" Duration="{StaticResource AnimFast}">
                                        <DoubleAnimation.EasingFunction><StaticResource ResourceKey="EaseOut"/></DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </Trigger.ExitActions>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Button.Style>
        <StackPanel>
            <Border CornerRadius="{StaticResource CardImageRadius}"
                    Background="{StaticResource ThumbPlaceholderBrush}"
                    Height="{StaticResource CardThumbHeight}" ClipToBounds="True">
                <Grid>
                    <Border Visibility="{Binding Cover, Converter={StaticResource NullToVisibility}}"
                            Background="{Binding Name, Converter={StaticResource StringToAvatarBrushConv}}"
                            CornerRadius="999" Width="64" Height="64"
                            HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="{Binding Name, Converter={StaticResource StringToInitialsConv}}"
                                   Foreground="White" FontSize="24" FontWeight="Medium"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <Image Source="{Binding Cover}"
                           Style="{StaticResource ThumbnailImage}" Stretch="UniformToFill" />
                </Grid>
            </Border>
            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Margin="2,8,2,0"
                       TextTrimming="CharacterEllipsis" />
            <TextBlock Text="{Binding VideoCountLabel}" Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="2,1,2,0" />
        </StackPanel>
    </Button>
</UserControl>
