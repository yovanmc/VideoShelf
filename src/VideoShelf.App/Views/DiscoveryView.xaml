<UserControl x:Class="VideoShelf.App.Views.DiscoveryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:views="clr-namespace:VideoShelf.App.Views">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24">

            <!-- Stats header -->
            <StackPanel Margin="0,0,0,16" Visibility="{Binding HasStats, Converter={StaticResource BoolToVisibility}}">
                <TextBlock Text="{Binding WatchedSummary}" FontSize="16" FontWeight="SemiBold" />
                <TextBlock Text="{Binding InProgressSummary}" Opacity="0.7" Margin="0,2,0,0"
                           Visibility="{Binding HasInProgress, Converter={StaticResource BoolToVisibility}}" />
                <ItemsControl ItemsSource="{Binding TopCreators}" Margin="0,8,0,0">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource SubtleFillBrush}" CornerRadius="12" Padding="10,4" Margin="0,0,8,0">
                                <TextBlock>
                                    <Run Text="{Binding Name}" />
                                    <Run Text=" · " />
                                    <Run Text="{Binding WatchedCount}" />
                                </TextBlock>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

            <!-- Empty state -->
            <TextBlock Text="Nothing here yet — add a source and start watching."
                       Opacity="0.7" Margin="0,40"
                       Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibility}}"/>

            <!-- Continue watching rail -->
            <StackPanel Visibility="{Binding HasContinueWatching, Converter={StaticResource BoolToVisibility}}">
                <TextBlock Text="Continue watching" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding ContinueWatching}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <views:VideoCard />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </StackPanel>

            <!-- Recommended creators rail -->
            <StackPanel Margin="0,24,0,0"
                        Visibility="{Binding HasRecommendedCreators, Converter={StaticResource BoolToVisibility}}">
                <TextBlock Text="Creators" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding RecommendedCreators}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate><views:CreatorCard Margin="0,0,12,0"/></DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </StackPanel>

            <!-- Recommended videos rail -->
            <StackPanel Margin="0,24,0,0"
                        Visibility="{Binding HasRecommendedVideos, Converter={StaticResource BoolToVisibility}}">
                <TextBlock Text="More to watch" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding RecommendedVideos}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate><views:VideoCard/></DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </StackPanel>

            <!-- Recently added rail -->
            <StackPanel Margin="0,24,0,0"
                        Visibility="{Binding HasRecentlyAdded, Converter={StaticResource BoolToVisibility}}">
                <TextBlock Text="Recently added" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding RecentlyAdded}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Command="{Binding PlayCommand}" Margin="0,0,12,0" Padding="0"
                                        Background="Transparent" BorderThickness="0" Width="200">
                                    <StackPanel>
                                        <Image Source="{Binding ThumbnailPath}" Height="112" Stretch="UniformToFill"/>
                                        <TextBlock Text="{Binding SeriesTitle}" FontWeight="SemiBold"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding EpisodeLabel}" Opacity="0.7" FontSize="12"/>
                                    </StackPanel>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </StackPanel>

            <!-- Recently watched rail -->
            <StackPanel Margin="0,24,0,0"
                        Visibility="{Binding HasRecentlyWatched, Converter={StaticResource BoolToVisibility}}">
                <TextBlock Text="Recently watched" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding RecentlyWatched}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Command="{Binding PlayCommand}" Margin="0,0,12,0" Padding="0"
                                        Background="Transparent" BorderThickness="0" Width="200">
                                    <StackPanel>
                                        <Image Source="{Binding ThumbnailPath}" Height="112" Stretch="UniformToFill"/>
                                        <TextBlock Text="{Binding SeriesTitle}" FontWeight="SemiBold"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding EpisodeLabel}" Opacity="0.7" FontSize="12"/>
                                    </StackPanel>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </StackPanel>

            <!-- Pick-a-tag section -->
            <StackPanel Margin="0,24,0,0"
                        Visibility="{Binding HasTags, Converter={StaticResource BoolToVisibility}}">
                <TextBlock Text="Pick a tag" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <ItemsControl ItemsSource="{Binding AvailableTags}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ToggleButton Content="{Binding Label}" IsChecked="{Binding IsSelected, Mode=OneWay}"
                                          Margin="0,0,8,8"
                                          Command="{Binding DataContext.ToggleTagCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                          CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <ItemsControl ItemsSource="{Binding TagResults}" Margin="0,8,0,0"
                              Visibility="{Binding HasTagResults, Converter={StaticResource BoolToVisibility}}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate><views:CreatorCard Margin="0,0,12,0"/></DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</UserControl>
