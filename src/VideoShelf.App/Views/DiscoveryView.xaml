<UserControl x:Class="VideoShelf.App.Views.DiscoveryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:views="clr-namespace:VideoShelf.App.Views">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24">

            <!-- Stats header -->
            <StackPanel Margin="0,0,0,16" Visibility="{Binding HasStats, Converter={StaticResource BoolToVisibility}}">
                <TextBlock Text="{Binding WatchedSummary}" FontSize="16" FontWeight="SemiBold" />
                <TextBlock Text="{Binding InProgressSummary}" Opacity="0.7" Margin="0,2,0,0"
                           Visibility="{Binding HasInProgress, Converter={StaticResource BoolToVisibility}}" />
                <ItemsControl ItemsSource="{Binding TopCreators}" Margin="0,8,0,0">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource Chip}" Margin="0,0,8,0">
                                <TextBlock>
                                    <Run Text="{Binding Name}" />
                                    <Run Text=" · " />
                                    <Run Text="{Binding WatchedCount}" />
                                </TextBlock>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

            <!-- Empty state -->
            <TextBlock Text="Nothing here yet — add a source and start watching."
                       Opacity="0.7" Margin="0,40"
                       Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibility}}"/>

            <!-- Continue watching rail -->
            <StackPanel Visibility="{Binding HasContinueWatching, Converter={StaticResource BoolToVisibility}}">
                <TextBlock Text="Continue watching" Style="{StaticResource TypeRailHeader}" Margin="0,0,0,8"/>
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding ContinueWatching}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}">
                                    <Border.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="Play next"
                                                      Command="{Binding PlacementTarget.Tag.PlayVideoNextCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding VideoId}"/>
                                            <MenuItem Header="Add to queue"
                                                      Command="{Binding PlacementTarget.Tag.EnqueueVideoCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding VideoId}"/>
                                        </ContextMenu>
                                    </Border.ContextMenu>
                                    <views:VideoCard />
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </StackPanel>

            <!-- Recommended creators rail -->
            <StackPanel Margin="0,24,0,0"
                        Visibility="{Binding HasRecommendedCreators, Converter={StaticResource BoolToVisibility}}">
                <TextBlock Text="Creators" Style="{StaticResource TypeRailHeader}" Margin="0,0,0,8"/>
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding RecommendedCreators}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate><views:CreatorCard Margin="0,0,12,0"/></DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </StackPanel>

            <!-- Recommended videos rail -->
            <StackPanel Margin="0,24,0,0"
                        Visibility="{Binding HasRecommendedVideos, Converter={StaticResource BoolToVisibility}}">
                <TextBlock Text="More to watch" Style="{StaticResource TypeRailHeader}" Margin="0,0,0,8"/>
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding RecommendedVideos}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}">
                                    <Border.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="Play next"
                                                      Command="{Binding PlacementTarget.Tag.PlayVideoNextCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding VideoId}"/>
                                            <MenuItem Header="Add to queue"
                                                      Command="{Binding PlacementTarget.Tag.EnqueueVideoCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding VideoId}"/>
                                        </ContextMenu>
                                    </Border.ContextMenu>
                                    <views:VideoCard/>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </StackPanel>

            <!-- Recently added rail -->
            <StackPanel Margin="0,24,0,0"
                        Visibility="{Binding HasRecentlyAdded, Converter={StaticResource BoolToVisibility}}">
                <TextBlock Text="Recently added" Style="{StaticResource TypeRailHeader}" Margin="0,0,0,8"/>
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding RecentlyAdded}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}">
                                    <Border.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="Play next"
                                                      Command="{Binding PlacementTarget.Tag.PlayVideoNextCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding VideoId}"/>
                                            <MenuItem Header="Add to queue"
                                                      Command="{Binding PlacementTarget.Tag.EnqueueVideoCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding VideoId}"/>
                                        </ContextMenu>
                                    </Border.ContextMenu>
                                    <views:VideoCard/>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </StackPanel>

            <!-- Recently watched rail -->
            <StackPanel Margin="0,24,0,0"
                        Visibility="{Binding HasRecentlyWatched, Converter={StaticResource BoolToVisibility}}">
                <TextBlock Text="Recently watched" Style="{StaticResource TypeRailHeader}" Margin="0,0,0,8"/>
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding RecentlyWatched}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}">
                                    <Border.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="Play next"
                                                      Command="{Binding PlacementTarget.Tag.PlayVideoNextCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding VideoId}"/>
                                            <MenuItem Header="Add to queue"
                                                      Command="{Binding PlacementTarget.Tag.EnqueueVideoCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding VideoId}"/>
                                        </ContextMenu>
                                    </Border.ContextMenu>
                                    <views:VideoCard/>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </StackPanel>

            <!-- Smart-view shelves -->
            <StackPanel Margin="0,24,0,0" Visibility="{Binding HasSmartShelves, Converter={StaticResource BoolToVisibility}}">
                <ItemsControl ItemsSource="{Binding SmartShelves}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,0,0,24">
                                <TextBlock Text="{Binding Name}" Style="{StaticResource TypeRailHeader}" Margin="0,0,0,8"/>
                                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                                    <ItemsControl ItemsSource="{Binding Items}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}">
                                                    <Border.ContextMenu>
                                                        <ContextMenu>
                                                            <MenuItem Header="Play next"
                                                                      Command="{Binding PlacementTarget.Tag.PlayVideoNextCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                                      CommandParameter="{Binding VideoId}"/>
                                                            <MenuItem Header="Add to queue"
                                                                      Command="{Binding PlacementTarget.Tag.EnqueueVideoCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                                      CommandParameter="{Binding VideoId}"/>
                                                        </ContextMenu>
                                                    </Border.ContextMenu>
                                                    <views:VideoCard/>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

            <!-- Favorites rail -->
            <StackPanel Margin="0,24,0,0"
                        Visibility="{Binding HasFavorites, Converter={StaticResource BoolToVisibility}}">
                <TextBlock Text="Favorites" Style="{StaticResource TypeRailHeader}" Margin="0,0,0,8"/>
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding Favorites}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}">
                                    <Border.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="Play next"
                                                      Command="{Binding PlacementTarget.Tag.PlayVideoNextCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding VideoId}"/>
                                            <MenuItem Header="Add to queue"
                                                      Command="{Binding PlacementTarget.Tag.EnqueueVideoCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding VideoId}"/>
                                        </ContextMenu>
                                    </Border.ContextMenu>
                                    <views:VideoCard/>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </StackPanel>

            <!-- Pick-a-tag section -->
            <StackPanel Margin="0,24,0,0"
                        Visibility="{Binding HasTags, Converter={StaticResource BoolToVisibility}}">
                <TextBlock Text="Pick a tag" Style="{StaticResource TypeRailHeader}" Margin="0,0,0,8"/>
                <ItemsControl ItemsSource="{Binding AvailableTags}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ToggleButton Content="{Binding Label}" IsChecked="{Binding IsSelected, Mode=OneWay}"
                                          Style="{StaticResource ChipToggle}"
                                          Margin="0,0,8,8"
                                          Command="{Binding DataContext.ToggleTagCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                          CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <ItemsControl ItemsSource="{Binding TagResults}" Margin="0,8,0,0"
                              Visibility="{Binding HasTagResults, Converter={StaticResource BoolToVisibility}}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate><views:CreatorCard Margin="0,0,12,0"/></DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</UserControl>
