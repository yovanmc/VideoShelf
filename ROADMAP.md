# VideoShelf — ROADMAP

> Source of truth for what to build next. A fresh session reads this, finds the topmost milestone that is **not ✅ Merged**, and acts. Follows the `/roadmap` workflow (Opus plans/researches · Sonnet implements · ping me at every phase handoff with the paste-ready prompt + which `/model` to switch to). This file — not the runbook's progress log — is the canonical state; keep state here, not in chat, and `/clear` between phases.

**Legend:** ✅ Merged · 🔨 In progress (on a branch, not yet merged) · 📝 Plan ready (execute next) · 🔬 Researching/Planning · [ ] Not started (plan first)

## Definition

Windows video library + player — "AudioShelf for video" on the VideoTriage stack (.NET 10 WPF + WPF-UI + LibVLCSharp play-everything + SQLite). Multi-source → section/**creator** → series/standalone → episode; section tags + discovery; watched/unwatched + resume; lean player + PiP; **strictly read-only for library files** (mutations are DB-only: the opt-in rename tool and — from v2 — the creator-art override, which stores a path to a user-chosen image and never writes into library folders). Self-contained — all playback/thumbnails/metadata from bundled libVLC, **no external tools (ffmpeg/HandBrake) on PATH**, no network for content. **v1 (M1–M6) ✅ complete; v2 (M7–M10) creator-centric redesign + immersive player ✅ complete; v3 (M11–M14) = polish & personalization — a shell/navigation & settings restructure (kill the always-on sidebar → gear/Settings), personal/stats home, subtitles & play-queue, and the app's first owned design system + light/dark theming.**

- Repo: https://github.com/yovanmc/VideoShelf (default branch `main`)
- Design specs: [`v1`](docs/superpowers/specs/2026-06-11-videoshelf-design.md) (revised 2026-06-11) · [`v2 creator redesign`](docs/superpowers/specs/2026-06-12-videoshelf-v2-creator-redesign.md) (M7–M10)
- Runbook (env, worktrees, CI, conventions): [`docs/superpowers/WORKFLOW-execution.md`](docs/superpowers/WORKFLOW-execution.md)

## Conventions (see runbook for detail)

- **Single-model, two-phase split (always Opus):** the session is **always Opus** (I drive Claude on mobile and **cannot switch models** — `/model` is a local-terminal-only picker). Opus orchestrates throughout: it brainstorms/plans, and in the build phase it delegates implementation to cheap **Sonnet subagents** (it does not hand-write production code). Opus also delegates file-reading/research to Explore/general subagents that return a digest. At every phase handoff, the finishing session pings me (PushNotification) with the exact paste-ready next-phase prompt; the ping asks only for **`/clear`** (never a `/model` switch).
- **Autonomy:** run each phase end-to-end without asking permission for safe, reversible actions; only stop for destructive/ambiguous/blocked, or the handoff ping. CI + tests + screenshots are the quality gate.
- `gh` is **not on PATH** → `& "C:\Program Files\GitHub CLI\gh.exe"`. Solution is `VideoShelf.slnx` (.NET 10 XML format). Test gate: `dotnet test VideoShelf.slnx -c Release --nologo -v q`.
- Work in **worktrees** under `.worktrees/`; **`gh pr merge` from the main repo root**, not the worktree. **Direct pushes to `main` are blocked** — every change (incl. docs/this file) ships via branch + PR.
- Commits: human author `yovanmc` + `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`. **No Codex trailer.** Merge `--merge` (no squash) from Phase 2 on. The previous-phase ROADMAP/runbook status flip rides on the next phase's branch.

## Milestones

| # | Phase | Status | Plan | PR | Notes |
|---|-------|--------|------|----|----|
| 1 | Foundation (Core indexer) | ✅ Merged | [M1](docs/superpowers/plans/2026-06-11-videoshelf-foundation.md) | [#1](https://github.com/yovanmc/VideoShelf/pull/1) | VideoExtensions, NaturalComparer, TitleParser, SectionGrouper, VideoShelfDb (WAL/FK, idempotent schema), FolderScanner, Library/Watch repos, ScanService. 32 Core tests. CI `build-and-test`. |
| 2 | App shell + library browse | ✅ Merged | [M2](docs/superpowers/plans/2026-06-11-videoshelf-app-shell.md) | [#4](https://github.com/yovanmc/VideoShelf/pull/4) | VideoShelf.App WPF shell + App.Tests; Core schema (`missing`/`added_at`/`resume_position`, idempotent guarded migration); browse read-model + search (#1) + sort (#3); folder picker, background scan coordinator, fail-safe libVLC thumbnail cache; browse VMs + shell views; unwatched badges (#6); missing-file dimming (#9); search jump-to. **71 tests.** Review caught + fixed a **Critical cross-thread `ObservableCollection`** bug (`ConfigureAwait(false)` on UI-bound load chains) — tests alone missed it. |
| 3 | Playback (player + PiP) | ✅ Merged | [M3](docs/superpowers/plans/2026-06-11-videoshelf-playback.md) | [#7](https://github.com/yovanmc/VideoShelf/pull/7) | Full libVLC playback: Core (resume r/w/clear, watched-clears-resume, `GetNextEpisode`, `SettingsRepository` auto-advance); `IPlaybackEngine` + `FakePlaybackEngine`; `ResumePolicy`; `PlayerViewModel` (transport, resume save/offer, end-of-media auto-watched + auto-next #10, track/chapter pickers incl. subtitles-off, keyboard map, screenshot #8, seek-preview #2 seam, missing-file + engine-error guards); launch-from-library + episode-row Play button; `MainViewModel` host + PiP routing; `SettingsViewModel` + auto-advance checkbox; DI; thin `LibVlcPlaybackEngine`; `PlayerView` (`VideoView` + additive overlay), `MiniPlayerWindow` (PiP with correct MediaPlayer re-parenting handoff). **140 tests (60 Core + 80 App).** Whole-branch review caught + fixed a **Critical off-thread libVLC-event hazard** (events now marshalled to the WPF dispatcher — `OnEnded` re-enters the player on auto-next, a deadlock risk from a libVLC callback thread); also wired the previously no-op fullscreen, fixed the seek-preview stale-frame cache, stop-on-switch, removed dead `NullPlaybackEngine`. Concrete engine + views are integration-only (screenshot-verified in Phase 6). |
| 4 | Discovery + tags | ✅ Merged | [M4](docs/superpowers/plans/2026-06-11-videoshelf-discovery.md) | [#10](https://github.com/yovanmc/VideoShelf/pull/10) | Home landing (Continue-watching / Recently-added / Recently-watched / For-you / Pick-a-tag rails) + section-level tagging via a dedicated section-detail view. Core: `TagRepository`, `resume_updated_at` guarded migration, pure `DiscoveryScoring` (14-day half-life affinity + unwatched-weighted scoring), `DiscoveryRepository` rail queries, `LibraryRepository.GetSection(id)`. App: Discovery rail/card/chip VMs + `DiscoveryViewModel`, `SectionDetailViewModel` (cached-tag autocomplete), `MainViewModel` Home/Browse/SectionDetail nav, `DiscoveryView`/`SectionDetailView`, `EnumToVisibility` converter, DI. **177 tests (83 Core + 94 App).** Whole-branch review caught two IMPORTANT issues (now fixed): scan didn't refresh Discovery rails; suggestion autocomplete hit SQLite synchronously per keystroke. Views remain screenshot-unverified until M6. |
| 5 | Opt-in rename tool | ✅ Merged | [M5](docs/superpowers/plans/2026-06-12-videoshelf-rename.md) | [#13](https://github.com/yovanmc/VideoShelf/pull/13) | Per-series opt-in rename: `VideoShelf.Core.Renaming` behind an `IFileSystem` seam — `CanonicalNamer` (`"<Base Title> <NN>.ext"` that re-parses to the same `(title,episode)` via `TitleParser`; standalones unnumbered), conflict-aware `RenamePlanner` (Unchanged/Ready/SourceMissing/TargetExists/DuplicateTarget/InvalidName), **manifest-first crash-safe `RenameExecutor`** (2-arg Move never overwrites, re-verify at apply time, tolerant Undo). `LibraryRepository.UpdateVideoPath` repaths `file_path`+`raw_filename`+path-keyed `grouping_overrides` in one tx off stable ids (watched/resume/tags survive). App: `RenameToolViewModel` (preview → inline edit → live re-plan → Apply → one-click Undo persisted in `settings.last_rename_manifest`, survives restart), `AppView.RenameTool` host, "Rename files…" entry on section-detail series cards. **200 tests (101 Core + 99 App).** Views integration-only — screenshot-verified in the Phase 6 sweep. |
| 6 | Harness + release polish | ✅ Merged | [M6](docs/superpowers/plans/2026-06-12-videoshelf-harness-release.md) | [#15](https://github.com/yovanmc/VideoShelf/pull/15) | Launch hooks (`--folder`/`--autostart`/`--done-signal` + `--data-dir`/`--view`/`--play`/`--seed-demo`, isolated DB), `HarnessRunner`, ffmpeg fixtures, GDI per-view sweep; **retroactive eyes-on sweep of all 7 views** paid the deferred debt and caught a **Critical** nav-overlap bug (all hosts rendered stacked — `EnumToVisibility` bound to `CurrentView` not `DataContext.CurrentView`, latent since M4) + player/mini overlay airspace; player now renders live video + transport, 6/7 views PASS. **PiP live-frame deferred** (libVLC vout doesn't migrate on MediaPlayer re-host). MSIX self-contained publish + signed (`CN=VideoShelf`, libVLC allowed) + CI `package` job asserting no media tools. **206 tests (101 Core + 105 App).** |
| | **— v2: creator-centric redesign + immersive player ([spec](docs/superpowers/specs/2026-06-12-videoshelf-v2-creator-redesign.md)) —** | | | | |
| 7 | Creator model + card system + shell foundation | ✅ Merged | [M7](docs/superpowers/plans/2026-06-12-videoshelf-creator-model.md) | [#17](https://github.com/yovanmc/VideoShelf/pull/17) | Section→**Creator** UI relabel (data model unchanged); `SectionSummary` gains **video count** + thumbnail seed; `creator_art` table + `CreatorArtRepository` + in-app `IImagePicker` (2nd **DB-only** mutation, path-only, no library writes); reusable `CreatorCardViewModel`/`CreatorsViewModel` + `CreatorCard`/`VideoCard` UserControls; **Browse = creator grid** + additive nav restyle; creator-art override entry on the creator page; sort/search `_pending`→CTS race fix. **222 tests (105 Core + 117 App).** Screenshot sweep caught + fixed a **latent TwoWay-binding crash** (`ProgressBar.Value`↔read-only `ProgressFraction`, latent since M4 — never triggered because no prior sweep seeded a continue-watching item). |
| 8 | Home + Search redesign (creator-centric) | ✅ Merged | [M8](docs/superpowers/plans/2026-06-12-videoshelf-home-search.md) | [#20](https://github.com/yovanmc/VideoShelf/pull/20) | Home funnel: Continue-watching + **Recommended creators** (creator cards) + **Recommended videos** (video cards, two sub-rails) + Recently-added/watched + Pick-a-tag (**migrated to creator cards**). Search = new `AppView.Search` from a persistent top search box → grouped Creators/Videos. New `CreatorCardFactory`; Core `SearchCreators`/`SearchVideos`/`GetRecommendedVideos`+`ScoreSections` refactor. **244 tests (118 Core + 126 App).** Sweep surfaced a **pre-existing** player-transport-bar bleed onto non-player views (reproduced on `main`; deferred to M10). |
| 9 | Creator page (Netflix-style) | ✅ Merged | [M9](docs/superpowers/plans/2026-06-12-videoshelf-creator-page.md) | [#22](https://github.com/yovanmc/VideoShelf/pull/22) | Section-detail redesigned into a creator page: **top hero banner** (creator art dimmed by an additive gradient, name + "N videos" + tag pills + art-override actions, fading into content) + **inline-accordion** series grid (multi-episode tiles expand in place w/ lazy episode load; standalone tiles play on click) + tag editor + per-series "Rename files…" (context-menu + expanded button — the v2 fold-in). VM-only: `SectionDetailViewModel.BackgroundImagePath`/`VideoCount` (override→seed, re-resolved on art set/clear) + `SeriesViewModel.Activate`/`IsExpanded`/`EpisodeCountLabel`; **no Core change**. **254 tests (118 Core + 136 App).** Sweep PASS (hero + tag pill + grid + expanded accordion + rename). |
| 10 | Immersive player redesign | ✅ Merged | [M10](docs/superpowers/plans/2026-06-12-videoshelf-immersive-player.md) | [#24](https://github.com/yovanmc/VideoShelf/pull/24) | Full-window edge-to-edge inline player + auto-hiding Fluent controls + polished stock-Slider scrubber (additive chapter ticks + seek-preview thumbnail + drag-to-seek). **In-window DRAGGABLE PiP** replaces MiniPlayerWindow (one VideoView; PiP shrinks the **host** to a corner panel so the app stays visible/usable behind it) — **fixes the deferred PiP black-frame bug**. **Transport-bar bleed FIXED** (player realized in the tree only while playing). Seek-preview off-screen decoder (fail-safe live-frame fallback). **259 tests (118 Core + 141 App)**; no Core change beyond additive `ChapterOption.StartSeconds`. Sweep PASS on all 8 views. **v2 (M7–M10) COMPLETE.** |
| | **— v3: polish & personalization (scoped 2026-06-12; UI/UX review 2026-06-12) —** | | | | |
| 11 | Shell, navigation & settings restructure | [ ] Not started | — | — | **From the senior UI/UX review** (owner dislikes the always-on sidebar). **Kill the persistent sidebar**; move all library/config controls behind a **gear → dedicated Settings screen** (sources add/remove + **Scan with feedback/last-scanned**, **Auto-play-next** toggle, a slot for the future theme toggle). Top chrome = `Home · Browse · Search · ⚙`. Add **back-navigation + active-nav state** (today there's no way back from a creator page); a **first-run/empty-library state** (centered "Add a source" CTA, not a buried sidebar button); **collapse the creator-page editorial controls** (Set image / Add tag / Rename files) behind an **Edit** affordance so a consumption page isn't half metadata-editor; make **Search** return Videos too (or scope its placeholder honestly); **remove the redundant sidebar Creators list** (Browse already does that); fix the **PiP transport clipping** at small width. Foundational chrome — **execute FIRST in v3** so M12–M14 build into the cleaned-up shell. Plan first. |
| 12 | Personal home & stats | [ ] Not started | — | — | Evolve Home into a personal, creator-centric landing: continue-watching ordered by recency with **chapter-granular resume** where chapters exist, plus **watch stats** (totals, per-creator counts, recently-finished). **Folds in `duration` population** (libVLC-probe on scan) so progress bars + stats are finally real (M7 noted `duration` is never populated → bars always empty). One cohesive surface — extend the existing Discovery Home, don't fragment it. (Review also flagged: rename the small-library "Recommended" rails honestly, add empty states.) Mirrors AudioShelf M11. Plan first. |
| 13 | Subtitles & play-queue | [ ] Not started | — | — | (a) **External subtitle sidecars** — auto-detect adjacent `.srt`/`.ass` next to the video + a manual "Add subtitle file…" in the player, fed into the existing subtitle-track picker via libVLC `Media.AddSlave`/`SetSpu`. Still NO downloading/online. (b) **Play-queue / play-all / up-next** — "Play all" across a creator (or a filtered/series set) building an explicit up-next queue beyond today's single-series auto-next, with a visible up-next + manual queue control. (Scoped explicit queue — NOT the infinite whole-library autoplay that stays OUT per v1 §13.) May split into two milestones if the plan is too big. Plan first. |
| 14 | Design system & theming (light/dark) | [ ] Not started | — | — | VideoShelf's **first owned design system**: promote/replace the borrowed VideoTriage `DesignTokens.xaml` into an owned token set, add a **light theme** + a **user toggle** in Settings (WPF-UI `ApplicationThemeManager`). **Absorbs the UI-review visual-consistency findings:** a real **icon system** (Fluent icons on every button + the player transport, which is text-only today), **ONE `CreatorCard` + ONE `VideoCard`** at fixed aspect ratios reused everywhere (cards differ in size across Home/Browse/Search/PiP today), a **button hierarchy** (primary/secondary/tertiary), unified **type scale & heading style** (mixed ALL-CAPS chrome vs Title Case), **chip-vs-button** distinction, visible **focus rings**, and bringing the **player into the app's Fluent chrome**. Done **LAST** so it themes M11–M13. Honor the theming rule (additive; never re-template a WPF-UI control for cosmetics). Mirrors AudioShelf M12. Plan first. |

## Decision log & gotchas

- **Feature-expansion (spec revised 2026-06-11, [PR #2](https://github.com/yovanmc/VideoShelf/pull/2)):** a research pass added 10 features + embedded track pickers and **reversed three earlier cuts** — **resume/continue-watching** (#5), **auto-play next episode within a series** (#10, settings-toggle, standalones never), and **embedded subtitle + audio-track pickers** (live from libVLC; no sidecars/downloads). Plus search (#1), seek-preview thumbnails (#2), sort (#3), recency rails (#4), unwatched badges (#6), chapter nav (#7), frame capture (#8), missing-file detection (#9). Still OUT: playback-speed, external subtitle sidecars/downloading, whole-library continuous play, online metadata scraping.
- **Execution workflow adopted ([PR #3](https://github.com/yovanmc/VideoShelf/pull/3)):** VideoTriage-style one-phase-per-run runbook; superseded as canonical state by this ROADMAP.
- **libVLC versions (pinned, restored clean):** `LibVLCSharp 3.9.7.1`, `VideoLAN.LibVLC.Windows 3.0.23.1`, `LibVLCSharp.WPF 3.9.7.1`. Do not move to 4.x. (Plan pins were older; bumped to latest 3.x that restores.)
- **Testability pattern (mirror for all libVLC/WPF work):** abstract native/UI behind an interface (`IThumbnailService`, `IPlaybackEngine`); put ALL logic in plain VMs/services unit-tested with a fake; keep the THIN concrete (`LibVlcThumbnailService`, `LibVlcPlaybackEngine`) and the Views/PiP **uncovered by unit tests** — they are integration, verified by the Phase 6 screenshot harness.
- **Visual verification DEFERRED to Phase 6:** no launch hooks/fixture exist yet, so the shell (Phase 2), player, PiP (Phase 3) and discovery (Phase 4) ship without an eyes-on pass. Phase 6 MUST do a full retroactive screenshot sweep. The Phase-2 cross-thread bug (below) shows tests alone are insufficient for UI correctness here.
- **Cross-thread `ObservableCollection` gotcha:** never `ConfigureAwait(false)` on a load chain that ends by mutating a UI-bound collection — the continuation lands on a thread-pool thread and WPF throws `NotSupportedException`. Keep heavy work inside `Task.Run` but resume on the captured UI context. A `SynchronizationContext` regression test guards this.
- **Theming rule (caused regressions in a sibling project):** never override/re-base a WPF-UI themed control's Style/ControlTemplate for cosmetics — additive (Opacity/RenderTransform) only.
- **PiP re-parenting (highest Phase-3 integration risk):** one libVLC `MediaPlayer` can host only one `VideoView`; on PiP toggle the inline surface must be cleared (`MediaPlayer = null`) before the mini-player claims it, else video blanks/freezes. Verify in the harness.
- **Seek-preview (#2) scope:** Phase 3 implements the testable seam (cached, fail-safe) and snapshots the live frame; a dedicated off-screen preview decoder is deferred to Phase 6 polish.
- **Git:** stale `.git/index.lock` → delete & retry; remove worktree before `git branch -d`; `gh pr merge` from the main repo root.
- **Minor deferred:** `LibraryViewModel` sort/search `_pending` task race (harmless); unused `db` param in `ScanService` (cosmetic — reserved for future transaction scoping).
- **History note:** Plan-1 + spec PRs (#1, #2) and the docs PRs (#3, #5) were squash-merged; feature phases use `--merge` from Phase 2 on.
- **M4 planning decisions (2026-06-11):** (1) **Home landing + Browse nav** — a new `AppView.Home` Discovery landing is the default; existing browse becomes `AppView.Browse`; `MainViewModel` gains the view switch + a `SectionDetail` host. (2) **Full weighted scoring** for For-you/Pick-a-tag — pure `DiscoveryScoring`: exponential recency decay (14-day half-life) builds per-tag affinity from watch history; section score = summed overlapping-tag affinity × (0.5 + 0.5·unwatched-ratio); For-you excludes already-watched sections. (3) **Dedicated section-detail view** hosts the tag editor (pills + autocomplete from `GetAllTags`). New Core: `resume_updated_at` column (guarded migration) to order continue-watching by recency, since `watch_events` clears resume on full-watch. Likely tiny Core add: `LibraryRepository.GetSection(long id)` for the section-detail load. Tags remain **section-level only** (spec §13).
- **M5 planning decisions (2026-06-12, plan [M5](docs/superpowers/plans/2026-06-12-videoshelf-rename.md)):** (1) **Naming = canonical + editable** — default proposal `"<Base Title> <NN>.ext"` built by a pure `CanonicalNamer` so it re-parses to the same `(title, episode)` via `TitleParser` (rescan re-groups identically); standalone series (single file) get no number; every target editable inline. (2) **Scope = per-series** — loads via `GetVideosForSeries(seriesId)`. (3) **Undo = in-app one-click** — manifest written to disk BEFORE any move; last manifest path persisted in `settings` key `last_rename_manifest` so Undo survives a restart. (4) **All logic in `VideoShelf.Core.Renaming` behind a new `IFileSystem` seam** (mirrors `IThumbnailService`/`IPlaybackEngine`): `RenamePlanner` (flags Unchanged/Ready/SourceMissing/TargetExists/DuplicateTarget/InvalidName), `RenameExecutor` (manifest-first, 2-arg `File.Move` never overwrites, re-verify at apply time, tolerant Undo), `CanonicalNamer`. (5) **DB repath = new `LibraryRepository.UpdateVideoPath(id, old, new)`** in one transaction — updates `videos.file_path` **+ `raw_filename`** (Search mirror) **+ path-keyed `grouping_overrides.file_path`**; watched/resume/tags key off ids → survive untouched. (6) **Entry point = section-detail series cards only** ("Rename files…" button → new `AppView.RenameTool` host via `SeriesViewModel.RenameRequested` bubbled through `SectionDetailViewModel`); browse-view series cards left unchanged this milestone (revisit Phase 6). (7) **Views screenshot-unverified** until the Phase 6 sweep — concrete `RealFileSystem` + `RenameToolView` are integration-only, as with prior phases. Note for executor: `InMemoryFileSystem` test helper lives in Core.Tests and App.Tests references it (csproj `<ProjectReference>`); if that doesn't build, copy the helper — STOP-and-report guidance is in the plan.
- **M5 shipped (2026-06-12, [PR #13](https://github.com/yovanmc/VideoShelf/pull/13)):** all 7 planning decisions landed as written; 200 tests (101 Core + 99 App), 0 failures. Durable facts: (a) **`Video` record positional order** is `(long Id, long SeriesId, string FilePath, int EpisodeNo, string RawFilename, string Format, double? Duration, string? ThumbnailPath, bool Watched, string AddedAt, bool Missing)` — confirmed against the real model (the plan's test helper matched). (b) **Rename safety pattern** (reuse for any future on-disk mutation): write the JSON undo manifest **before** any move (`WriteAtomic` = write `.tmp` then Move), use the **2-arg `File.Move`** (throws on existing target — never overwrites), **re-verify source-exists/target-free at apply time** (disk may have changed since planning), and make Undo **tolerant** of partially-applied batches (skip entries whose move never happened / whose old path is reoccupied). (c) **`UpdateVideoPath(id, old, new)`** updates three things in one tx: `videos.file_path` + `videos.raw_filename` (the Search mirror — easy to forget) + path-keyed `grouping_overrides.file_path`; everything else (watched/resume/tags) keys off stable `videos.id` and survives a repath untouched. (d) **Cross-project test helper**: `VideoShelf.App.Tests.csproj` **already had** a `<ProjectReference>` to `VideoShelf.Core.Tests.csproj`, so `InMemoryFileSystem` was shared directly (no copy needed). (e) **Adding a ctor param to `MainViewModel`** (the new `RenameToolViewModel`) required updating 3 existing App.Tests construction sites (`MainViewModelTestFactory` + two test files) — expected fan-out, not a regression. (f) **Rename views remain screenshot-unverified** — Phase 6 must include `RenameToolView` + the section-detail "Rename files…" entry in the retroactive sweep.
- **M6 planning decisions (2026-06-12, plan [M6](docs/superpowers/plans/2026-06-12-videoshelf-harness-release.md)):** Three sequential workstreams. (A) **Launch-hook contract** — the user's three core hooks (`--folder` set single source + scan, `--autostart` run scan/load pipeline + begin `--play`, `--done-signal` write-when-rendered-and-settled) **plus three support flags**: `--data-dir` (SAFETY — isolated throwaway SQLite DB so the harness never touches the real library), `--view <Home|Browse|SectionDetail|RenameTool|Player|PiP|Settings>`, `--play <clip>`, `--seed-demo` (mark-watched + resume + tag so Discovery rails/badges render). Pure unit-tested `HarnessOptions.Parse` (unknown args ignored, forward-compatible); a `HarnessRunner` drives the UI to the requested view, settles, and always writes the signal (try/catch→signal so the capture script can't hang). (B) **Capture = relaunch-per-view + GDI screen-grab** (mirrors the proven VideoTriage `Drive-AndCapture` pattern — captures REAL pixels incl. live libVLC video for Player/PiP, unlike `RenderTargetBitmap` which renders the VLC airspace surface black). One launch per nav state → wait on signal → `CopyFromScreen` → PNG. Retroactive sweep covers all 7 views with explicit per-view acceptance criteria; **screenshots verified by a Sonnet subagent returning a TEXT verdict, never loaded into the controller**; fixes are **additive-only** per the theming rule. (C) **MSIX = self-contained `dotnet publish` (win-x64) → hand-authored `AppxManifest.xml` + placeholder logo assets → `makeappx pack` → `signtool` self-signed cert** (`CN=VideoShelf`, must match manifest `Publisher`; CI artifact, not store-submittable). **No `.wapproj`** (pure CLI, slnx-friendly). The **"no media tools" assertion is a name-denylist** (`ffmpeg/ffprobe/ffplay/HandBrake*/mkv*/mencoder/x264/x265…`) that **explicitly allows libVLC** (`libvlc.dll`/`libvlccore.dll`/plugins — the bundled engine); CI `package` job runs it on the publish output before packing. Fixtures generated by **ffmpeg (dev-only tool, NEVER shipped)** into a gitignored scratch dir. Flags for the executor to STOP-and-report on: the data-dir DB-path wiring and the source-add/scan/seed/play VM+repo APIs (described by behavior, not verbatim signatures).
- **M4 shipped (2026-06-12, [PR #10](https://github.com/yovanmc/VideoShelf/pull/10)):** all three decisions landed as planned (`GetSection(id)` was indeed added). Durable facts: (a) **`resume_updated_at TEXT`** column on `videos`, maintained by `SetResumePosition` (set), `ClearResumePosition` + `WatchRepository.SetWatched(true)` (null); migration uses the repo's idempotent `EnsureColumn(conn, table, col, def)` helper (`pragma_table_info` guard) — NOT inline try/catch. (b) **DB-access convention**: Core repos open a connection per call via `db.Open()` and parameterize with **`$`-prefixed** placeholders (Microsoft.Data.Sqlite also accepts `@`). (c) **`SeriesSummary` lives in `VideoShelf.Core.Models`**, `EpisodeView` likewise — not in `Storage`. (d) **`SectionDetailViewModel` ctor takes 4 deps** (`LibraryRepository, TagRepository, WatchRepository, IThumbnailService`) because it builds `SeriesViewModel(s, library, watch, thumbnails)` exactly like `SectionViewModel`. (e) New **`EnumToVisibility`** converter (key `EnumToVis`) gates the three nav hosts; existing boolean-visibility key is **`BoolToVisibility`**. The existing browse markup was **moved** into a nav-gated host in `Views/MainWindow.xaml` (not deleted); the `PlayerView` overlay is untouched. (f) **`ScanAndReload` now also calls `Discovery.LoadAsync()`** so Home rails refresh after a scan; `SectionDetailViewModel` caches `GetAllTags()` off-thread for keystroke-time suggestion filtering (no per-keystroke DB hit) — both were whole-branch-review fixes. Discovery/section-detail **views are still screenshot-unverified** — Phase 6 must include them in the retroactive sweep.
- **M6 shipped (2026-06-12, [PR #15](https://github.com/yovanmc/VideoShelf/pull/15)):** harness + retroactive sweep + MSIX. **206 tests (101 Core + 105 App).** Durable facts & lessons: (a) **The sweep paid off immediately** — a **Critical** latent bug (since M4) had ALL nav hosts rendering stacked on top of each other: the `EnumToVisibility` host bindings in `Views/MainWindow.xaml` used `{Binding CurrentView, RelativeSource=Window}` (Window has no such property → path fails → `Visibility` falls back to its **default `Visible`**). Fix = `{Binding DataContext.CurrentView, …}`. **Lesson: WPF binding-path failures silently fall back to the DP default; unit tests never catch this — only an eyes-on sweep does.** (b) **DB-path seam**: `AddVideoShelf(string? dataDirOverride = null)` → `new AppPaths(dataDirOverride)`; `AppPaths.DatabasePath` = `<root>\library.db`. The harness isolates via `--data-dir`. (c) **Real harness APIs** wired in `HarnessRunner`: add source `LibraryRepository.UpsertSource(root, name)`; scan `MainViewModel.ScanAndReloadCommand.ExecuteAsync(null)`; nav `OpenSectionAsync(id)`/`OpenRenameToolAsync(SeriesViewModel)`/`CurrentView = AppView.x`; play `MainViewModel.PlayEpisode(EpisodeView)` — **must be a REAL `LibraryRepository.GetEpisodes(seriesId)` row** (synthetic `EpisodeView(VideoId=0)` trips the missing-file guard → "File not found"); PiP `MainViewModel.IsPictureInPicture = true`. **No separate Settings `AppView`** — settings controls live in the always-visible sidebar. (d) **Scanner is FLAT** (`FolderScanner`: each immediate subfolder = a section, videos one level deep; nested per-series folders are ignored); series are grouped from **filenames** by `TitleParser` = base title + **first integer token** (`"Big Buck Bunny 1"` → ep 1; `"…S01E01"` does NOT group). Fixtures must reflect this. (e) **GDI capture gotchas (now baked into `Run-VisualSweep.ps1`)**: wait for `IsWindowVisible`; settle ~5s for the **WPF-UI Mica backdrop to compose** (else all-black); a **`TOPMOST`→`NOTOPMOST` bring-to-front toggle** (a *permanent* topmost whites out LibVLCSharp.WPF's overlay foreground window); and **a composited/unlocked desktop is required** — a locked/remote session captures 100% black (no API can fix it). (f) **Player overlay airspace**: overlay controls must be **children of `VideoView`** (its `Content`), not siblings, or the airspace HWND occludes them; `MiniPlayerWindow` must assign its `MediaPlayer` in **`Loaded`** (not the ctor) so the surface HWND exists. (g) **MSIX**: `Build-Msix.ps1` self-contained publish bundles libVLC (`libvlc.dll`/`libvlccore.dll` + 325 plugins); signed self-signed `CN=VideoShelf` (must equal manifest `Publisher`); CI `package` job runs `Assert-NoMediaTools.ps1` (libVLC-allowed denylist) before packing. (h) **DEFERRED — PiP live frame**: re-hosting a *playing* libVLC `MediaPlayer` onto the mini-player's second `VideoView` leaves the running vout bound to the old HWND, so the mini renders **black** (chrome/handoff/return all work). Needs a vout-restart (save position → `Stop`/`Play` → seek) with flicker/position tradeoffs — its own follow-up, not done here per the user's call.
- **v2 scoped (2026-06-12, brainstorming pass → [spec](docs/superpowers/specs/2026-06-12-videoshelf-v2-creator-redesign.md)):** v1 (M1–M6) complete; v2 is a **creator-centric redesign + immersive player** across four milestones (M7–M10), M7 planned (📝). Locked decisions: (1) **Creator = top-level subfolder = today's Section, relabeled** — the `sections` table and four-level hierarchy are **unchanged**; "creator" is a presentation concept (lowest data-model churn). (2) **Card philosophy** — a reusable **`CreatorCard`** (thumbnail + name + "N videos" count) wherever a creator is surfaced; a **`VideoCard`** (video thumbnail) wherever an individual video is surfaced; used consistently on Home/Search/creator pages so the app reads as one cohesive surface. (3) **Creator thumbnail = user-override → representative frame** — precedence is an in-app **user-assigned image** (stored in a new `creator_art` table, **path only**, picked via `Microsoft.Win32.OpenFileDialog`), else the existing libVLC seed-frame. **No folder-image scan, no online scraping.** This override is the **second DB-only mutation** (alongside rename) — it never writes into library folders, so "strictly read-only for library files" holds. (4) **Home = a curated funnel**, not a static grid: leads users into **Continue-watching** (video cards, ordered by `resume_updated_at`) or **For-you / you-may-like** (mix of creator + video cards via the existing 14-day half-life `DiscoveryScoring`). (5) **Creator page = Netflix-style** — faded creator-art background, series-grouping grid + standalones, tags, override entry. (6) **Search = mixed** creator + video cards, grouped. (7) **Immersive player (M10)** — edge-to-edge video, auto-hiding overlay controls, polished Fluent transport; cohesive visual language applied app-wide; **additive theming only** (the standing WPF-UI rule). (8) **All four v1 loose threads folded in**: PiP black-frame vout-restart + seek-preview off-screen decoder → M10; browse/creator rename entry → M9; sort/search `_pending` race → M7. (9) **Still OUT** (unchanged v1 §13): playback-speed, external subtitle sidecars, whole-library continuous play/queue, online metadata scraping, transcoding, streaming, casting, file deletion.
- **M7 shipped (2026-06-12, [PR #17](https://github.com/yovanmc/VideoShelf/pull/17)):** all 9 plan tasks landed; **222 tests (105 Core + 117 App)**, 0 failures. Durable facts & lessons:
  - **(a) Latent TwoWay-binding crash — the sweep's headline catch.** `ProgressBar` (and any `RangeBase`) binds `Value` **TwoWay by default**; binding it to a read-only VM property (`ProgressFraction`, get-only) throws `InvalidOperationException` *the instant the binding activates*. This was latent since M4 in the inline continue-watching template and never fired because **no prior screenshot sweep ever populated the continue-watching rail** (seed-demo set resume on `episodes[1]` of `sections[0]`'s first series, and the alphabetically-first "Movies" fixture held only single-episode standalones). Fix = `Value="{Binding ProgressFraction, Mode=OneWay}"`. **Rule: pin OneWay on any `RangeBase.Value`/`Slider`/`ProgressBar` bound to a computed/read-only property.**
  - **(b) Harness verification gap closed.** `HarnessRunner.SeedDemoAsync` now seeds watched+resume on the **richest (most-episode) series across all sections**, not blindly `sections[0]`'s first series — so the continue-watching rail is actually exercised by the sweep. Also: the on-disk fixture cache (`%TEMP%\vs-fixtures`) is **stale-prone** — `Generate-Fixtures.ps1` skips regen if the folder exists, and an older nested layout (`Shows\Big Buck Bunny\…S01E01.mp4`) silently won't scan (the scanner is **flat**: videos must sit *directly* under the section subfolder). Run `Generate-Fixtures.ps1 -Force` after changing fixtures.
  - **(c) Persistent sidebar is by design.** The left sidebar (sources + the now-"CREATORS" list) renders on **every** view including Browse/Home — the nav-gated hosts live in the content column to its right. A reviewer unaware of this will mis-flag it; judge only the main content area.
  - **(d) Transitional UX:** replacing the Browse host wholesale (per plan) **removed the inline search box + sort combo** from Browse. The `LibraryViewModel` search/sort logic remains (and the `_pending`→CTS race was fixed here); **M8's Search redesign restores a search surface.**
  - **(e) `duration` is never populated app-wide** (`UpsertVideo` takes no duration; the scanner doesn't probe it), so `ProgressFraction` is always 0 → the continue-watching progress bar renders *empty* for fixtures and real libraries alike. Pre-existing limitation, identical in the old inline template. If a filled bar is ever wanted, duration population is its own task (candidate for M10's player work).
  - **(f) Real signatures confirmed:** `SectionDetailViewModel` load method is **`LoadAsync(long sectionId)`** (not `OpenAsync`); the loaded id is the public **`SectionId`** property; its ctor is now 6 params `(LibraryRepository, TagRepository, WatchRepository, IThumbnailService, CreatorArtRepository, IImagePicker)`. `MainViewModel` section-open nav is **`OpenSectionAsync(long)`** (sets `CurrentView=SectionDetail` + loads); Browse-reload path is the `[RelayCommand]` `ScanAndReload()` (now also `Creators.LoadAsync`). `IThumbnailService.GetThumbnailPathAsync(string, CancellationToken cancellationToken)`. `RangeBase.Value` TwoWay-default per (a).
  - **(g) Review hardening applied:** superseded sort/search tasks are wrapped so their `OperationCanceledException` is swallowed (no unobserved faults); `CreatorsViewModel.LoadAsync` pre-fetches all `creator_art` overrides **inside** the `Task.Run` (off the UI thread) before building cards; `SetCreatorArt`/`ClearCreatorArt` guard `SectionId <= 0` (would otherwise FK-violate `creator_art`). The cross-thread `ObservableCollection` rule held (no `ConfigureAwait(false)` on `Creators` mutation chains).
- **M8 planning decisions (2026-06-12, plan [M8](docs/superpowers/plans/2026-06-12-videoshelf-home-search.md)):** user-locked the Home/Search shape via one batched `AskUserQuestion`. (1) **Home keeps ALL rails** (not a minimal funnel): Continue-watching → **Recommended creators** (creator cards) → **Recommended videos** (video cards) → Recently-added → Recently-watched → Pick-a-tag. (2) **For-you = two homogeneous sub-rails** (creators then videos), NOT one interleaved score-mixed rail — avoids mixed card sizes + a template selector. The old `ForYou` `SectionCardViewModel` rail is **replaced**; `SectionCardViewModel.cs` left in place as a deferred-cleanup candidate (removing it would churn its tests). (3) **Pick-a-tag migrated to the new card system** — tag-chip results now render as **creator cards** (`CreatorCardViewModel`) instead of `SectionCardViewModel`. (4) **Search = dedicated `AppView.Search`** opened from a **persistent top search box** in the nav chrome (typing flips `CurrentView=Search` via `MainViewModel` observing `Search.Query`); results are two grouped sections (Creators / Videos). Chosen over restoring inline Browse search (M7 removed that box) or a live flyout (airspace risk). (5) **New Core queries:** `LibraryRepository.SearchCreators`(→`SectionSummary`, mirrors `GetSectionSummaries` + a `display_name LIKE`) and `SearchVideos`(→`RecencyItem`, matches `raw_filename` OR series `base_title`, excludes missing); `DiscoveryRepository.GetRecommendedVideos`(unwatched videos from the For-you-recommended sections, ranked) via a refactored private `ScoreSections(now)` shared with `GetForYou`. Recommended-videos reuse `RecencyItem`/`RecencyCardViewModel` (no model proliferation; `ProgressFraction` bar renders empty as elsewhere since `duration` is unpopulated). (6) **New `CreatorCardFactory`** (`CreatorArtRepository`+`IThumbnailService`) centralizes "build a creator card from a `SectionSummary` (apply art override, kick off `LoadImageAsync`)" so Home + Search produce identical cards; it mirrors `CreatorsViewModel`'s existing art-lookup (executor reads that file for the exact call). `CreatorsViewModel` left untouched (lower risk than refactoring it onto the factory). (7) **Harness** gains a `--view Search` case (seed a query matching a fixture creator, await `WaitForIdleAsync`) so the M8 sweep covers it. (8) Ctor fan-out expected: `MainViewModel` gains a `SearchViewModel` param; `DiscoveryViewModel` gains a `CreatorCardFactory` param → update `MainViewModelTestFactory` + direct construction sites (as in M5/M7). Views screenshot-verified in Task 8's sweep (subagent text verdict, never load PNGs into the controller).
- **M8 shipped (2026-06-12, [PR #20](https://github.com/yovanmc/VideoShelf/pull/20)):** all 9 plan tasks landed as written; **244 tests (118 Core + 126 App)**, 0 failures. Durable facts & lessons:
  - **(a) Plan matched reality almost exactly** — every STOP-and-report guard passed clean. The only repeated micro-fix: `RecencyItem` lives in namespace **`VideoShelf.Core.Discovery`** (not `…Models`), so files newly referencing it (LibraryRepository, SearchViewModel) needed `using VideoShelf.Core.Discovery;` — the plan's `using` lists omitted it. `CreatorArtRepository.GetArtPath(long sectionId)` was the exact existing single-id call (no convenience method needed). `IThumbnailService` is in `VideoShelf.App.Services`.
  - **(b) Shared `ScoreSections(now)`** — `GetForYou` and the new `GetRecommendedVideos` both consume one private ordered-scoring helper; recommended-videos = unwatched videos from the recommended sections, ranked by section order (section ids inlined into the `IN (...)` clause — safe, they're trusted DB integer keys, never user input). Recommended videos reuse `RecencyItem`/`RecencyCardViewModel` (no new model).
  - **(c) `DiscoveryView.xaml` has NO local `<UserControl.Resources>` block** — converters (`BoolToVisibility`) resolve from **app-level `App.xaml`**, not a per-view header. The plan assumed a local header to copy; the executor correctly matched the real pattern (SearchView declares no local resources either). Lesson for future view work: this codebase registers converters app-wide.
  - **(d) Search box = `ui:TextBox` with `PlaceholderText`** (WPF-UI 4.3.0 supports it; `SectionDetailView` already used it). Typing flips `CurrentView=Search` via `MainViewModel` subscribing to `Search.PropertyChanged` on `Query`. Search debounces with `Task.Delay(150)` + a `_opCts`/`WaitForIdleAsync` test hook (mirrors `LibraryViewModel`).
  - **(e) Ctor fan-out, as predicted:** `MainViewModel` gained a `SearchViewModel` param and `DiscoveryViewModel` a `CreatorCardFactory` param → updated `MainViewModelTestFactory` + the direct construction sites in `MainViewModelTests`/`MainViewModelPlaybackTests`. `SectionCardViewModel.cs` left on disk (now unused — deferred cleanup).
  - **(f) Sweep-seed gap (same class as M7's):** the harness `SeedDemoAsync` tagged only one section, so the new **Recommended creators/videos rails rendered empty** in the first sweep (GetForYou needs a *watched, tagged* section to build affinity AND a *distinct unwatched* section sharing the tag). Fix: tag **every** section with "demo" — then the watched section seeds affinity and the others surface as recommendations. After that, the sweep confirmed all M8 rails render (Continue-watching → Recommended creators card → Recommended videos → Recently-added/watched) and Search shows the populated query box + a Creators group. **Lesson: when a milestone adds a new discovery rail, extend the seed so the sweep actually exercises it.**
  - **(g) PRE-EXISTING player-bar bug surfaced (NOT M8, deferred to M10):** the sweep found the player **transport bar (seek `0s…0s` + Play/Pause/volume/Screenshot/Fullscreen/Mini-player/Close) renders at the bottom of every non-player view** (Home/Search/Browse) when nothing is playing. **Reproduced on plain `main`** in a throwaway worktree — M8 touches no player-visibility code (`IsInlinePlayerVisible => IsPlayerVisible && !IsPictureInPicture` and the `MainWindow.xaml` `PlayerView` binding are byte-identical to `main`; `PlayerView` does not override its DataContext, so it is not a binding-path fallback — `IsPlayerVisible` is genuinely true with nothing played, pointing at a libVLC-airspace/overlay issue). Belongs to **M10 (immersive player redesign — edge-to-edge + auto-hiding controls)**; M6/M7 sweeps missed it. Filed as a follow-up.
  - **(h) Flaky test noted:** `BrowseFanoutTests.SelectingSection_loads_series_with_episodes_and_thumbnails` failed once in a full run, passed in isolation and on every re-run — a pre-existing parallel-execution flake (thumbnail/temp-DB), unrelated to M8. Candidate for a test-isolation follow-up.
- **M9 planning decisions (2026-06-12, plan [M9](docs/superpowers/plans/2026-06-12-videoshelf-creator-page.md)):** the **section-detail surface IS the creator page** — M9 is a visual redesign + two small VM additions (no Core change; reuses existing repo methods). User-locked via one batched `AskUserQuestion`: (1) **Art background = a top HERO BANNER** (creator art dimmed by an *additive* gradient `Border`+`LinearGradientBrush` over an `Image` — never a control re-template — with name + "N videos" + applied tag pills + "Set image…/Use default" overlaid, fading into the grid below). NOT a full-page background. (2) **Series expansion = inline ACCORDION** — series render as tiles in a `WrapPanel` grid; a **multi-episode** tile toggles `IsExpanded` in place (episodes **lazy-load on first expand** via the existing `LoadEpisodesAsync`); a **standalone** (1-episode) tile **plays on click**. New `SeriesViewModel.Activate` command (standalone-play / multi-expand) + `IsExpanded` + `EpisodeCountLabel`. (3) **Rename fold-in** — rename is inherently **per-series** (no creator-level rename), so each tile gets a right-click **ContextMenu → "Rename files…"** (covers standalones) plus a visible "Rename files…" button in the multi-episode expanded panel; the creator page (reached from Browse on card click) is where it lives — this satisfies the v2 "browse/creator rename entry" fold-in. (4) **Hero data + faded background**: `SectionDetailViewModel` gains `[ObservableProperty] BackgroundImagePath` (precedence **override → section seed frame** via `IThumbnailService`, mirroring `CreatorCardViewModel.LoadImageAsync`) + `VideoCount`, captured in `LoadAsync` from `library.GetSection(id)` (a `SectionSummary` with `VideoCount`+`ThumbnailSeedPath`; fallback to `GetSectionSummaries().First(...)`). `SetCreatorArt`/`ClearCreatorArt` become `async Task` to **re-resolve the background** (XAML `…Command` bindings still resolve as the generated `AsyncRelayCommand` — existing tests must switch to `ExecuteAsync`). (5) **Harness**: the `--view SectionDetail` sweep now **expands the first multi-episode series** (`Activate`) before settling so the accordion-open state is captured. Views screenshot-verified in Task 6 (subagent text verdict, never load PNGs into the controller). Expected ctor/test impact is App-only.
- **M9 shipped (2026-06-12, [PR #22](https://github.com/yovanmc/VideoShelf/pull/22)):** all 7 plan tasks landed; **254 tests (118 Core + 136 App)**, 0 failures; **no Core change**. Durable facts & lessons:
  - **(a) `LibraryRepository.GetSection(long)` returns a lean `Section` record** (`Id, SourceId, FolderName, DisplayName` only — NOT a `SectionSummary`). For `VideoCount`/`ThumbnailSeedPath` use `library.GetSectionSummaries().First(s => s.SectionId == id)` (the plan's documented fallback — the executor hit it as predicted). The hero `BackgroundImagePath` resolves **override → section seed via `IThumbnailService` → null** (mirrors `CreatorCardViewModel.LoadImageAsync`); `SetCreatorArt`/`ClearCreatorArt` are now `async Task` so they re-resolve it.
  - **(b) The previous `SectionDetailViewModel.LoadAsync` did NOT eager-load series thumbnails/episodes** — M9 added `_ = svm.LoadThumbnailAsync(...)` per series so the grid tiles show art; episodes still **lazy-load on accordion expand** via `SeriesViewModel.Activate` → `LoadEpisodesAsync`. Standalone tiles play on activate (load-then-play first episode).
  - **(c) App-level converters confirmed (again):** `BoolToVisibility`/`MissingToOpacity` are registered in `App.xaml` and resolve as `{StaticResource}` in any view; design tokens (`CardRadius`, `ControlRadius`, `CardImageRadius`, `ThumbPlaceholderBrush`, `SubtleFillBrush`, `AccentBrush`, `SectionHeader`) live in `Resources/DesignTokens.xaml`. `SectionDetailView.xaml` declares only the DesignTokens merged dict in its header (no local converter keys). All keys existed — no substitutions.
  - **(d) Sweep-seed gap (same class as M7/M8):** the `--view SectionDetail` capture ran WITHOUT `--seed-demo`, so the section had no tags and the first sweep showed an empty hero tag-pill area (false FAIL). Added `--seed-demo` to the section-detail sweep entry → the "demo" tag pill renders and watched/resume state shows. **Lesson: every sweep view that surfaces tags/discovery state must pass `--seed-demo`.**
  - **(e) The pre-existing player-bar bug (M10) clips the bottom of the creator page** — the expanded accordion's lowest episode rows are obscured by the transport bar bleeding onto the view. Confirmed out-of-scope (reproduced on `main` during M8); the accordion itself renders correctly above it. Another reason M10's immersive-player pass should clear it.
  - **(f) Rename fold-in landed as planned:** per-series right-click `ContextMenu → "Rename files…"` on every tile (covers standalones) + a visible "Rename files…" button in multi-episode expanded panels. No creator-level rename (rename is inherently per-series). Reuses the existing `SeriesViewModel.RequestRename` → `SectionDetailViewModel.RenameRequested` → `MainViewModel.OpenRenameToolAsync` chain untouched.
- **M10 planning decisions (2026-06-12, plan [M10](docs/superpowers/plans/2026-06-12-videoshelf-immersive-player.md)):** the final v2 milestone — an immersive Movies-&-TV-style player. User-locked two shape decisions, then a follow-up PiP question. (1) **Host = inline overlay, FULL-WINDOW** — the player overlays the **entire** window (over title bar + nav + sidebar) while playing, not just the content columns. (2) **The transport-bar BLEED fix** (pre-existing, reproduced on `main`, deferred through M8/M9): **only realize the `PlayerView`/`VideoView` in the visual tree while playing.** Mechanism = the player host becomes a `ContentControl x:Name="PlayerHost"` (Grid.RowSpan over the whole window); `MainWindow.xaml.cs` sets `PlayerHost.Content = <PlayerView>` on `IsPlayerVisible` true and `= null` on false — tearing it down destroys LibVLCSharp.WPF's separate **foreground/overlay HWND** (which hosts the overlay controls and bleeds them over other views even when the parent is `Collapsed`; this overlay window is the same one the M6 TOPMOST→NOTOPMOST toggle whitens). (3) **PiP = IN-WINDOW + DRAGGABLE** (user's follow-up question, answered yes) — replaces the separate `MiniPlayerWindow`. PiP is now a **mode** of the inline player: the **same single `VideoView`** shrinks to a ≈360×203 floating `Border` (anchored via a `TranslateTransform`, dragged from its top strip, clamped to window bounds), driven by `DataTrigger`s on `MainViewModel.IsPictureInPicture`; `RootGrid.Background` goes `{x:Null}` in PiP so clicks fall through to the content behind. **Because the one libVLC `MediaPlayer` never re-hosts onto a second `VideoView`/window, the deferred PiP black-frame bug is fixed for free** — `MiniPlayerWindow.xaml(.cs)` is **deleted** and all `MainWindow.xaml.cs` PiP-window orchestration removed. (4) **Auto-hiding controls** — a `DispatcherTimer` in `PlayerView` code-behind toggles `PlayerViewModel.AreControlsVisible` (shown on `MouseMove`/key/scrub, hidden after 3s idle while playing; never hidden when paused/scrubbing/erroring/resuming, or when `AutoHideSuppressed` — a harness hook so the sweep captures the transport). (5) **Polished scrubber, ADDITIVE only** (theming rule): the stock `Slider` is bound `Value="{Binding Player.ScrubPosition, Mode=TwoWay}"` (settable → no M7 read-only-binding crash); **drag-to-seek** wired via `Thumb.DragStarted/Delta/Completed` routed events (template-safe, no re-template) → `BeginScrub`/`UpdateScrubPreviewAsync`/`CommitScrub` on the VM; **chapter tick marks** drawn on a `Canvas` layered over the slider from a new `ChapterOption.StartSeconds` (mapped from libVLC `ChapterDescription.TimeOffset/1000`); a **seek-preview thumbnail** `Image` popup positioned to the scrub point. **Buffered markers are N/A** for local-only playback (whole file is buffered) — markers = chapter ticks + thumb. (6) **Seek-preview off-screen decoder** (folded-in v1 thread) — `LibVlcPlaybackEngine.TryGeneratePreviewFrameAsync` gets a real implementation: a hidden, audio-disabled second `MediaPlayer` seeks to the hovered second and snapshots **that** frame, with a **fail-safe fallback to the live-frame snapshot** so it can never regress/throw; if headless `TakeSnapshot` yields nothing in-env, the fallback ships and it's documented (a conscious PASS, matching M3/M6). (7) **Scrubber range uses engine `LengthSeconds`** (libVLC-populated), NOT the never-populated `duration` column. (8) **Tasks:** T1 engine seam (chapter start times + off-screen decoder + Fake), T2 PlayerViewModel scrub/preview/auto-hide logic **+ ~5 unit tests**, T3 immersive `PlayerView` rewrite (auto-hide + scrubber + preview + draggable PiP), T4 full-window host + bleed fix + delete `MiniPlayerWindow`, T5 harness (`AutoHideSuppressed`; PiP renders in-window over Home) + GDI sweep verified by a **subagent text verdict** (headline check: **no transport bleed** on Home/Browse/Search; **PiP shows live video not black**). Branch `feat/immersive-player`. Expected ~259 tests (254 + 5), **no Core change beyond the additive `ChapterOption.StartSeconds`**.
- **M10 shipped (2026-06-12, [PR #24](https://github.com/yovanmc/VideoShelf/pull/24)):** all 5 tasks landed; **259 tests (118 Core + 141 App)**, 0 failures; no Core change beyond the additive `ChapterOption.StartSeconds`. **v2 (M7–M10) is COMPLETE.** Durable facts & lessons — most are **airspace corrections to the plan's PiP design** (the plan's mechanism was partly wrong and was fixed during the sweep loop):
  - **(a) THE BIG PIP LESSON — in-window PiP must shrink the HOST, not use transparency over the live video.** The plan's design (keep `PlayerHost` full-window, shrink an inner `PlayerShell` Border, make `RootGrid.Background={x:Null}` so the app shows behind) **does NOT work**: a transparent WPF surface layered over a `vlc:VideoView` (an `HwndHost`) renders the surround **solid black** — WPF cannot composite app content behind a native airspace child via transparency. **Working design:** the `PlayerHost` `ContentControl` **itself** shrinks to a ≈360×203 corner via a `Style`/`DataTrigger` on `IsPictureInPicture` (layout-only `Width`/`Height`/`HorizontalAlignment` setters in `MainWindow.xaml`); `PlayerView` simply fills its host in both modes (its own PiP size-trigger and transparent-RootGrid trigger were **removed**). Then the rest of the window is ordinary, fully-visible WPF (Home) — verified PASS (live PiP video + Home sidebar/rails visible behind).
  - **(b) NEVER move an HwndHost/VideoView with `RenderTransform`.** The plan dragged the PiP panel via a `TranslateTransform` (`PipTranslate`) on the VideoView's container — this **desyncs LibVLCSharp.WPF's airspace tracking and makes libVLC spill the video into a separate top-level "VLC (Direct3D11 output)" window** (and black-out the embedded surface). **Fix:** position/drag the panel with **layout `Margin`** (HwndHost repositions its child HWND on layout changes; it ignores RenderTransform). The final drag adjusts the **host's** `Margin` (`this.Parent as FrameworkElement`), clamped to `Window.GetWindow(this)` bounds.
  - **(c) The transport-bar BLEED fix worked exactly as planned and is the milestone's headline win.** `MainWindow.xaml.cs` `UpdatePlayerHost(bool)` sets `PlayerHost.Content = new PlayerView{…}` on `IsPlayerVisible` true and `= null` on false; `PlayerView.OnUnloaded`→`DetachSurface()`. Nulling the content destroys the `VideoView` + its foreground/overlay HWND, so the control bar no longer bleeds onto Home/Browse/Search (reproduced on `main` since M8; now PASS on all 6 non-player views).
  - **(d) Scrubber additive-only held.** Stock `Slider` bound TwoWay to a **settable** `[ObservableProperty] ScrubPosition` (no M7 read-only-binding crash); drag-to-seek via `Thumb.DragStarted/Delta/Completed` routed events (no template override); chapter ticks are `Rectangle`s on a `Canvas` layered over the slider from `ChapterOption.StartSeconds` (libVLC `ChapterDescription.TimeOffset/1000`). Seek-preview popup `Image` binds `SeekPreviewPath`; `RequestSeekPreviewAsync` caches per rounded second.
  - **(e) Off-screen seek-preview decoder shipped with its fail-safe** — hidden muted second `MediaPlayer` seeks + snapshots, falling back to the live-frame snapshot (`_player.Media?.Mrl` round-trips through `new Uri(...)`; `_previewPlayer` disposed before `_player`).
  - **(f) Harness/sweep gotchas:** `Run-VisualSweep.ps1` / `Generate-Fixtures.ps1` use **pwsh 7 syntax (`?.`)** — must run under `pwsh`/the PowerShell-7 tool, NOT `powershell.exe` (5.1 → parse error). `HarnessRunner` sets `Player.AutoHideSuppressed = true` for player/pip so the sweep captures the transport, and for PiP sets `CurrentView = AppView.Home` so the floating panel is shot over real content. The harness GDI capture grabs `$proc.MainWindowHandle`; **right after a clean rebuild (`rm obj/bin`) libVLC's transient D3D11 vout window can be captured instead of the app shell** — a cold-start/timing artifact, not an app bug (warm re-runs embed cleanly). Verify the sweep by a **Sonnet subagent text verdict**, never load PNGs into the controller.
  - **(g) Minor non-blocking follow-up:** `MainViewModel.IsInlinePlayerVisible` (+ its two `OnXChanged` partials) is now **dead** (the old XAML binding/`InlinePlayer` x:Name are gone) — safe to delete later. Also a benign unguarded `_previewPlayer` read/write vs `Dispose()` race (native handles + try/catch make it low-severity).
- **v3 SCOPED (2026-06-12, batched `AskUserQuestion`):** v1 + v2 fully shipped; v3 = **polish & personalization** (3 plan-first milestones, M11–M13). User picked direction "polish & personalization" and three themes: personal/stats home, subtitles & play-queue, and light/dark theming + design system (explicitly **did NOT** pick playback-speed → deferred to a possible later milestone). Ordering mirrors AudioShelf v3 — **personalization first, theming LAST** so the new design system paints all finished surfaces. **Locked scope decisions:** (1) **M11 Personal home & stats** extends the **existing Discovery Home** (one cohesive surface, not a new fragmented page): creator-centric continue-watching by recency + chapter-granular resume where chapters exist + watch stats (totals/per-creator/recently-finished). **Folds in `duration` population** (libVLC-probe on scan, the natural home for it since stats/progress need real durations — fixes the standing M7 gap where `UpsertVideo` takes no duration so every `ProgressFraction` bar renders empty). (2) **M12 Subtitles & play-queue** = (a) external `.srt`/`.ass` **sidecars** auto-detected adjacent to the file + a manual picker, fed to the existing subtitle-track UI via libVLC `Media.AddSlave`/`SetSpu` (still **no online/downloading**), and (b) an **explicit play-all / up-next queue** across a creator or filtered set, extending the current single-series auto-next (NOT infinite whole-library autoplay — that stays OUT per v1 §13). Flagged to **split into two milestones if the plan is too big**. (3) **M13 Design system & theming** is VideoShelf's first **owned** token system (today it borrows VideoTriage `DesignTokens.xaml`, dark-only) + a **light theme** + a settings toggle via WPF-UI `ApplicationThemeManager`; **theming-rule still binds** (additive only, never re-template a WPF-UI control for cosmetics — caused regressions in a sibling). (4) **Still OUT (unchanged v1 §13):** playback-speed (deferred this round), external subtitle **downloading**/online metadata scraping, infinite whole-library continuous play, transcoding, streaming, casting, file deletion. (5) **Standing conventions carry forward:** always-Opus + Sonnet subagents, screenshot sweep verified by a subagent **text verdict** (sweep needs `pwsh`-7 + an unlocked composited desktop), one milestone per phase, ROADMAP flips ride the feat branch (ROADMAP already on main). **NEXT (superseded — see UI/UX review below): plan M11.**
- **UI/UX review → new M11 + renumber (2026-06-12):** owner flagged a strong dislike of the always-on left sidebar ("most of it belongs behind a gear/settings screen"). Ran two parallel senior-perspective subagent critiques (a **senior UI developer** + a **senior UX designer**) over all 8 captured screens; they **converged**. Synthesis drove a **new v3 milestone + a renumber** (all v3 was still unplanned, so renumbering is clean): **M11 Shell/navigation & settings restructure (NEW, execute FIRST)** → M12 Personal home & stats → M13 Subtitles & play-queue → M14 Design system & theming (last). Rationale for shell-first: it's foundational chrome that the home redesign (M12) and theming (M14) should be built *into*, not retrofitted onto. **Key converged findings & their milestone home:**
  - **→ M11 (structure/IA):** the sidebar conflates *config* (Add source / Scan / source list + Remove), a *playback preference* (Auto-play-next toggle), and *navigation* (Creators list) and pins all of it to every screen — config + preference move behind a **gear → Settings screen**, the **Creators list is redundant with Browse** (both open the same creator page) so it's removed, and the sidebar effectively disappears. No **back-nav / active-nav state** today (can't return from a creator page except by re-clicking Browse). No **first-run/empty-library CTA** (Add-source is buried). The creator page **mixes consumption with metadata-editing** (Set image / Add tag / Rename at full weight) → collapse behind an **Edit** affordance. **Search** promises "creators and videos" but returns only creators → return videos or scope the placeholder. **PiP transport clips** its buttons at small width → responsive/min-width.
  - **→ M14 (visual system / design tokens):** **no icons anywhere** — the **player transport is text-only buttons** and the **player doesn't share the app's Fluent chrome**; the **CreatorCard/VideoCard render at different sizes/aspect ratios** across Home/Browse/Search/PiP (should be ONE component each at a fixed ratio); inconsistent **heading casing** (ALL-CAPS chrome vs Title Case), **button hierarchy**, **chip-vs-button** styling, **count-badge blue ≠ accent**, and **no visible focus rings**. These are design-system work → folded into M14's scope (above).
  - **Deferred/optional (logged, not committed):** the UX designer proposed replacing the floating PiP with a **persistent now-playing mini-bar** (Spotify/Plex pattern) — noted but NOT adopted, since the owner specifically asked for and we just shipped the in-window **draggable** PiP (M10); revisit only if desired. Also flagged for later: **Browse sort/filter** (fine at 2 creators, design in before it grows), **scan auto-trigger/feedback** (partly in M11's Settings). **NEXT: plan M11 (Shell, navigation & settings restructure).**
- **Future ideas / Icebox (2026-06-12):** a second, deeper four-lens subagent UX review (product feature-gaps · accessibility · power-user/large-library · interaction/motion-delight) gathered **net-new** ideas for **beyond v3** (filtered against M11–M14 and against already-shipped M10 player work). Captured in **[`docs/superpowers/specs/2026-06-12-videoshelf-future-ideas-icebox.md`](docs/superpowers/specs/2026-06-12-videoshelf-future-ideas-icebox.md)** — NOT committed scope; feeds v4 scoping. **Cross-cutting top bets:** (1) **video-level tags → smart filters/saved views**, (2) **library health: missing-file relink + maintenance dashboard**, (3) **multi-select + bulk actions**, (4) **in-player depth** (playback-speed, chapter/bookmark panel, A-B repeat, audio-track picker — all libVLC-native), (5) an **accessibility program** (several WCAG Level-A/legal-risk gaps: UIA names/roles, full keyboard reachability, color-independent state), (6) **command palette (Ctrl+K)**, (7) a **delight layer** (skeleton loaders, toasts+undo, up-next countdown, series-complete moment, transitions). The doc proposes candidate v4 milestone groupings (Organize · Power & scale · Library health · Player depth · Accessibility · Delight).
